<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron's V8 needs writable-executable pages and unsigned JIT memory;
       without these three the hardened runtime kills the renderer on launch. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- The shell talks to the backend over HTTP/WebSocket, which counts as an
       outgoing network connection even when the backend is on localhost. -->
  <key>com.apple.security.network.client</key>
  <true/>
</dict>
</plist>
